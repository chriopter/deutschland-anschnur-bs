<p align="center">
  <img src="medien/zeichen.png" alt="Deutschland Anschnur Muschel Dienstzeichen" width="300">
</p>

# Deutschland Anschnur Muschel

Eine tabellengeführte Muschel zur Verdeutschung von Befehlen auf bestehenden Einix-/Leinux-Systemen.

Einige angelsächsische Restbegriffe verbleiben aufgrund technischer Zuständigkeitsbindung bis zur späteren Verdeutschungsverfügung im Bestand.

## Einrichtung

```bash
git clone https://github.com/chriopter/deutschland-anschnur-muschel.git ~/.deutschland-anschnur-muschel
echo 'alias muschel="$HOME/.deutschland-anschnur-muschel/bin/muschel"' >> ~/.bashrc
. ~/.bashrc
```

Die Muschel meldet verfügbare Erneuerungen beim Start.

**Muschel**

```bash
muschel
muschel verzeichnis liste .
```

## BEFEHLE

<!-- BEFEHLE:ANFANG -->

### Muschel (eingebaut)

<details>
<summary><strong><code>hilfe</code></strong></summary>

#### `hilfe`

zeigt diese dienstliche Auskunft

- Ausführung: `eingebaut: hilfe`
- Ordnung: keine weiteren Angaben

</details>

<details>
<summary><strong><code>verlassen</code></strong></summary>

#### `verlassen`

schließt die Muschel ordnungsgemäß

- Ausführung: `eingebaut: verlassen`
- Ordnung: keine weiteren Angaben

</details>

<details>
<summary><strong><code>muschel</code></strong></summary>

#### `muschel neu laden`

lädt die Befehlsakten neu

- Ausführung: `eingebaut: neu_laden`
- Ordnung: keine weiteren Angaben

</details>

### Grundbestand

<details>
<summary><strong><code>verzeichnis</code></strong></summary>

#### `verzeichnis ort`

zeigt den derzeitigen Ort

- Ausführung: `/usr/bin/pwd`
- Ordnung: keine weiteren Angaben

#### `verzeichnis liste`

zeigt einen Verzeichnisbestand

- Ausführung: `/usr/bin/ls -la`
- Ordnung: weitere Angaben werden angehängt

#### `verzeichnis wechsel`

wechselt den derzeitigen Ort

- Ausführung: `eingebaut: cd`
- Ordnung: genau eine weitere Angabe

</details>

<details>
<summary><strong><code>datei</code></strong></summary>

#### `datei lesen`

liest eine Datei

- Ausführung: `/usr/bin/cat`
- Ordnung: genau eine weitere Angabe

#### `datei zeigen`

zeigt Dateiangaben

- Ausführung: `/usr/bin/stat`
- Ordnung: genau eine weitere Angabe

</details>

<details>
<summary><strong><code>netz</code></strong></summary>

#### `netz stand`

zeigt Netzanschriften

- Ausführung: `/usr/bin/ip addr show`
- Ordnung: keine weiteren Angaben

#### `netz weg`

zeigt Netzwege

- Ausführung: `/usr/bin/ip route show`
- Ordnung: keine weiteren Angaben

#### `netz pruefen`

prüft die Erreichbarkeit

- Ausführung: `/usr/bin/ping -c 4`
- Ordnung: genau eine weitere Angabe

#### `netz prüfen`

prüft die Erreichbarkeit

- Ausführung: `Alias auf: netz pruefen`
- Ordnung: weitere Angaben werden angehängt

</details>

<details>
<summary><strong><code>dienst</code></strong></summary>

#### `dienst liste`

zeigt laufende Dienste

- Ausführung: `/usr/bin/systemctl --type=service --state=running --no-pager`
- Ordnung: keine weiteren Angaben

#### `dienst stand`

zeigt einen Dienststand

- Ausführung: `/usr/bin/systemctl status --no-pager`
- Ordnung: genau eine weitere Angabe

</details>

<details>
<summary><strong><code>system</code></strong></summary>

#### `system stand`

zeigt Systemstand

- Ausführung: `/usr/bin/sh -c uname -a; uptime; free -h; df -h`
- Ordnung: keine weiteren Angaben

#### `system kennung`

zeigt Betriebssystemkennung

- Ausführung: `/usr/bin/cat /etc/os-release`
- Ordnung: keine weiteren Angaben

#### `system neustarten`

startet neu

- Ausführung: `/usr/bin/systemctl reboot`
- Ordnung: keine weiteren Angaben

#### `system ausschalten`

schaltet aus

- Ausführung: `/usr/bin/systemctl poweroff`
- Ordnung: keine weiteren Angaben

</details>

### Hapsmann (pacman)

<details>
<summary><strong><code>hapsmann</code></strong></summary>

#### `hapsmann erneuern`

erneuert Paketbestand und System

- Ausführung: `/usr/bin/pacman -Syu`
- Ordnung: keine weiteren Angaben

#### `hapsmann suchen`

sucht verfügbare Pakete

- Ausführung: `/usr/bin/pacman -Ss`
- Ordnung: genau eine weitere Angabe

#### `hapsmann zeigen`

zeigt ein eingerichtetes Paket

- Ausführung: `/usr/bin/pacman -Qi`
- Ordnung: genau eine weitere Angabe

#### `hapsmann fernzeigen`

zeigt ein verfügbares Paket

- Ausführung: `/usr/bin/pacman -Si`
- Ordnung: genau eine weitere Angabe

#### `hapsmann installieren`

richtet Pakete ein

- Ausführung: `/usr/bin/pacman -S`
- Ordnung: weitere Angaben werden angehängt

#### `hapsmann entfernen`

entfernt Pakete samt Folgebestand

- Ausführung: `/usr/bin/pacman -Rns`
- Ordnung: weitere Angaben werden angehängt

#### `hapsmann bestand`

listet eingerichtete Pakete

- Ausführung: `/usr/bin/pacman -Q`
- Ordnung: keine weiteren Angaben

#### `hapsmann aufraeumen`

räumt den Paketzwischenspeicher auf

- Ausführung: `/usr/bin/pacman -Sc`
- Ordnung: keine weiteren Angaben

#### `hapsmann aufräumen`

räumt den Paketzwischenspeicher auf

- Ausführung: `Alias auf: hapsmann aufraeumen`
- Ordnung: keine weiteren Angaben

</details>

### Hafener (docker)

<details>
<summary><strong><code>hafener</code></strong></summary>

#### `hafener bilder`

zeigt Hafenerbilder

- Ausführung: `/usr/bin/docker images`
- Ordnung: keine weiteren Angaben

#### `hafener bestand`

zeigt Hafenvorgänge

- Ausführung: `/usr/bin/docker ps`
- Ordnung: weitere Angaben werden angehängt

#### `hafener lauf`

startet einen Hafenvorgang

- Ausführung: `/usr/bin/docker run`
- Ordnung: weitere Angaben werden angehängt

#### `hafener zusammensetz zieh`

zieht zusammengesetzte Hafenerbilder

- Ausführung: `/usr/bin/docker compose pull`
- Ordnung: weitere Angaben werden angehängt

#### `hafener zusammensetz hoch`

startet zusammengesetzte Hafendienste

- Ausführung: `/usr/bin/docker compose up`
- Ordnung: weitere Angaben werden angehängt
- Optionen:
  - `--daemonisiere` → `--detach`
  - `--dämonisiere` → `--detach`
  - `--neu-bauen` → `--build`

#### `hafener zusammensetz runter`

beendet zusammengesetzte Hafendienste

- Ausführung: `/usr/bin/docker compose down`
- Ordnung: weitere Angaben werden angehängt

#### `hafener zusammensetz tagebuch`

zeigt Hafentagebuch

- Ausführung: `/usr/bin/docker compose logs`
- Ordnung: weitere Angaben werden angehängt
- Optionen:
  - `--schwanz` → `--tail`
  - `--verfolge` → `--follow`

</details>

<!-- BEFEHLE:ENDE -->
